{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "osx": {
                "command": "${workspaceFolder}/build.sh",
            },
            "linux": {
                "command": "${workspaceFolder}/build.sh",
            },
            "windows": {
                "command": "${workspaceFolder}/build.ps1",
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
